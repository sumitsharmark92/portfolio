version: '3.8'

services:
  sync-server:
    build: .
    container_name: sync-server
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
